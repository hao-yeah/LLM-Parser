# LLM-Parser

A program for estimating the weights and computational costs of LLMs, developed in C++, and using JSON as the input interface file.

Drag target json file to the exe or 

```cmd
llm-parser path\of\your\xxx.json
```

Now you can export the information gathered from any source to the json file shown in the example and perform analysis.
